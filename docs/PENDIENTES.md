# Pendientes – Windsor CRM

Documento para anotar lo que queda por hacer y los errores a resolver. Actualizar al cerrar cada sesión.

---

## Pendiente (próxima sesión)

### **Bot WhatsApp — Siguiente paso inmediato**
- [ ] Probar flujo completo en LAB: saludo → catálogo → correo → info → CTA A/B → inscripción/clase prueba
- [ ] Probar fase `asesor`: pedir día/hora → capturar teléfono → confirmar llamada
- [ ] Verificar que `info_enviada` da información correcta del programa elegido desde la BASE
- [ ] Agregar **fechas de inicio** de cada programa a la BASE (RAG)
- [ ] Definir y cargar el prompt maestro del bot desde la pestaña `BOT` en el CRM
- [ ] Probar bot en producción (WhatsApp real) con el flujo completo
- [ ] Probar etiquetas claras de fases en UI, sin cambiar el flujo del bot: `accion` = "Información enviada · esperando decisión", `cerrado` = "Flujo del bot finalizado", `seguimiento` = "Pendiente de retomar"
- [ ] Diseñar el relevo humano → bot: al volver a BOT, elegir fase de reanudación (dudas, siguiente paso, inscripción o cerrar), sin inferirla por cada mensaje humano
- [x] Crear un panel manual de **Reactivaciones sugeridas** para revisar, editar, enviar o descartar registros de `mensajes_pendientes`; no automatizar envíos antes de validar textos y proveedor — **hecho 2026-09-03:** ya no admite texto libre, obliga a elegir uno de los templates aprobados por Meta (preview de solo lectura), sin auto-envío. Desplegado.

### **Técnico / Producto**
- [ ] Ejecutar Fase 1 y Fase 2 de `docs/IMPLEMENTACION_ESCUELA.md`
- [ ] Revisar manualmente el CRM con `docs/CRM_QA_CHECKLIST.md`
- [ ] Diseñar la evolución de `FLOWS` a un constructor visual tipo canvas
- [ ] Validar en uso real la sincronía completa `lead.stage` ↔ `whatsapp_conversaciones.fase`
- [ ] Integrar al CRM el envío de template inicial para leads `walkin`
- [ ] Terminar activación de Meta Cloud API en producción
  - **Sesión 2026-05-20 — avance:**
  - ✅ Webhook verificado: `https://crm.windsor.edu.mx/api/whatsapp/webhook` responde correctamente
  - ✅ `WHATSAPP_PROVIDER=meta` activado en Vercel y desplegado
  - ✅ `META_WHATSAPP_VERIFY_TOKEN=windsor-waba-2026` configurado
  - ✅ `META_PHONE_NUMBER_ID=1049147001613376` configurado
  - ✅ `META_WHATSAPP_TOKEN` actualizado con token permanente (usuario del sistema)
  - ✅ Webhook fields: `messages` suscrito en Meta Developer Portal
  - ⏳ **ESPERANDO RESPUESTA DE META SUPPORT:** caso abierto en Direct Support (Dev: Phone Number & Registration) el 2026-05-20. Esperando que reseteen el contador de verificación.
  - ⏳ **Siguiente paso:** esperar ~30 min y solicitar código por llamada de voz con:
    `TOKEN=$(cat salescrm/tokenMeta.txt) && curl -X POST "https://graph.facebook.com/v20.0/1049147001613376/request_code" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"code_method": "VOICE", "language": "es"}'`
  - ⏳ Luego registrar con: `curl -X POST "https://graph.facebook.com/v20.0/1049147001613376/register" -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" -d '{"messaging_product": "whatsapp", "pin": "CODIGO"}'`
  - ✅ `tokenMeta.txt` protegido en `.gitignore` — se queda como respaldo local
- [ ] Correr migración de `lead_activities` en Supabase y validar que el historial persista entre sesiones
- [ ] Afinar el guardado de notas del lead para registrar un solo evento al guardar/blurear, no por cada tecla
- [x] Correr `supabase/respuestas_rapidas_custom.sql` en Supabase — hecho 2026-09-19, tabla creada y verificada por API.
- [ ] **Bug sin corregir — marcar un lead como "Inscrito" en el pipeline cierra su conversación de WhatsApp y el bot deja de contestarle.** `app/crm.jsx:1584` (`getConversationPhaseForStage`) mapea `inscrito → fase 'cerrado'` y `moveStage()` actualiza `whatsapp_conversaciones` directo. Si el lead vuelve a escribir (o toca un botón), `webhook/route.ts:2880` lo trata como "caso cerrado que reescribió" — solo notifica al asesor por WhatsApp y registra `lead_activities`, pero **no le responde nada al lead**. Es la dirección opuesta del bug ya corregido el 17-sep (`9321fa6`, que era bot→stage; este es CRM→conversación, código distinto, nunca tocado). Caso real: Alina López (+527471592228, Licenciatura en Inglés) tocó "Sí, envíame la info" del template de bienvenida y nunca recibió nada — Harold le mandó la info a mano desde el CRM el 19-sep mientras se decide el fix. **Pendiente decidir con Harold:** quitar el mapeo `inscrito: "cerrado"` (recomendado) o dejar que el bot conteste igual aunque la conversación esté cerrada.

### **Mobile / UX — Revisión pendiente**
- [ ] Probar el CRM completo en iPhone/Android después de los cambios responsive y de rendimiento del 2026-08-28
- [ ] Validar en celular: Kanban sin rebote horizontal, filtros sin desbordamiento y tabla LISTA con scroll horizontal táctil
- [ ] Validar en celular una cuenta con muchas conversaciones: scroll fluido, abrir → volver → reabrir el mismo chat y llegada de mensajes nuevos sin parpadeo
- [ ] Aplicar/verificar `supabase/migration_conversaciones_visto.sql` en producción y comprobar que el punto verde solo aparece cuando el último mensaje es del prospecto
- [ ] Verificar que el menú hamburguesa se cierra al hacer scroll o al tocar fuera
- [ ] Revisar que las demás vistas (KANBAN, LISTA, AGENDA, BASE, FLOWS, BOT) se ven bien en móvil — sólo se ajustaron STATS, LAB BOT, modales y CONVERSACIONES por ahora

---

## Pendientes Marketing

### **Inmediato (Esta semana)**
- [ ] Configurar IDs reales de analytics (GA4, Meta Pixel, Hotjar)
- [ ] Lanzar primera campaña de LinkedIn Ads ($200)
- [ ] Setup email sequences en Resend
- [ ] Agendar primeras 5 demos con prospects
- [ ] Testear landing page con tráfico real

### **Corto Plazo (Próximo mes)**
- [ ] Escalar campañas a $1,000/mes
- [ ] Crear primer case study real
- [ ] Lanzar webinar mensual
- [ ] Optimizar landing page basado en datos
- [ ] Implementar programa de referidos

### **Mediano Plazo (Próximos 3 meses)**
- [ ] Expandir a Colombia y Argentina
- [ ] Crear mobile app (iOS/Android)
- [ ] Implementar advanced reporting
- [ ] Alcanzar $10,000 MRR
- [ ] Certificaciones SOC 2

---

## Pendientes Técnicos

- **Secuencia de la plática:** ver `docs/FLUJO_BOT_WHATSAPP.md` (fases, mensajes del bot, qué espera en cada paso y sincronía con el CRM).
- **Twilio productivo:** ya no se usa sandbox; el número oficial `+5217474785589` está activo en producción.
- **Migración a Meta:** el código ya acepta `WHATSAPP_PROVIDER=meta` y el webhook soporta verificación `GET` + eventos de mensajes de WhatsApp Cloud API.
- **QA operativo:** usar `npm run crm:check` antes de cerrar cambios importantes.
- **Ruta de implementacion:** seguir `docs/IMPLEMENTACION_ESCUELA.md` para preparar el despliegue real en la escuela.

---

## Hecho (referencia rápida)

- **Flujo bot simulado y definido (2026-03-30):** saludo → programa (catálogo hardcodeado) → correo → info_enviada (RAG) → accion (CTA A/B) → dudas/inscripcion/clase_prueba. Mensajes hardcodeados: catálogo, inscripción lics, clase de prueba. Fase asesor con planteles, horarios y captura de teléfono. Interceptor para bloquear que GPT genere el catálogo por su cuenta.
- **Flujo del bot WhatsApp por fases:** saludo → programa → correo → info_enviada → dudas → accion → cerrado/perdido. Se actualiza `fase` y lead (stage, nombre, email, curso).
- **Twilio productivo activo:** sender oficial `Instituto Windsor` en línea con webhook apuntando a `https://crm.windsor.edu.mx/api/whatsapp/webhook`.
- **Separar oferta educativa:** Solo programas de *idiomas* (niños/adultos) ofrecen clase de prueba; el resto ofrece llamada o inscripción. Config en webhook (`tieneClasePrueba`) y CTA distinto según programa (link agendar vs contacto).
- **Sync pipeline CRM ↔ fase WhatsApp:** Al avanzar en WhatsApp se actualiza el stage del lead y al mover etapas en CRM también se empuja una fase coherente a la conversación ligada.
- **RLS y panel CONVERSACIONES:** Contestar, Tomar control y Volver a BOT funcionando (variables Twilio en Vercel; modo humano = el bot no responde hasta "Volver a BOT").
- Memoria del bot: contexto del lead se envía a RAG en cada mensaje.
- Modo humano: "Tomar control" / "Volver a BOT" y cuadro para responder como vendedor en la vista CONVERSACIONES.
- Flows por palabra clave (FLOWS en CRM): reglas con match, respuesta fija o RAG; guardado en `whatsapp_flows`.
- Configuración BOT en CRM: nueva pestaña `BOT` para centralizar identidad y comportamiento del bot en `whatsapp_flows.config.bot_prompt`, sin reemplazar todavía el flujo actual.
- LAB BOT en CRM: simulador conversacional para escenarios `ads` y `walkin`, conectado a la `BASE` por RAG para probar respuestas sin afectar producción.
- LAB BOT mejorado: ya consulta la `BASE`, muestra respuestas con mejor estructura y valida mejor correos mal capturados como programa, pero todavía queda pendiente el cambio fluido entre ofertas dentro del mismo chat.
- Campo `fase` y columnas `modo_humano`, `tomado_por` en `whatsapp_conversaciones`.
- Vista de conversaciones mejorada: búsqueda, filtros, badges, lead ligado y responsable.
- Modal del lead mejorado: siguiente paso, última actividad y timeline.
- Envío directo desde modal del lead: botón `Enviar información` por API de WhatsApp, sin depender de WhatsApp Web.
- Timeline comercial persistente: nueva tabla `lead_activities` y registro de etapas, reasignación, citas y respuestas del agente.
- Agenda mejorada: validación de fecha, status operativos y paso automático a `propuesta`.
- Refactor inicial del CRM: conversaciones, agenda, lead modal, kanban, lista y modales extraídos a `components/crm/`.
- Scripts de soporte: `npm run whatsapp:check`, `npm run crm:check`.
- Checklist operativa: `docs/CRM_QA_CHECKLIST.md`.
- Compatibilidad dual de WhatsApp: envío y webhook soportan `Twilio` y `Meta Cloud API` vía `WHATSAPP_PROVIDER`.

---

- **CRM mobile-ready (2026-03-30):** viewport meta + PWA (manifest.json, iconos). Hamburger menu con dropdown `position:absolute top:100%` anclado al header. Stats 2col, LAB BOT stack vertical, modales bottom-sheet en mobile. ConversationsPanel con toggle list/chat y botón "← Conversaciones". Header limpio en mobile: oculta "CRM v1.0", badge ADMIN, reduce título a 22px.
- **UX y rendimiento móvil (2026-08-28):** se corrigió el layout forzado a ~1400 px; filtros responsivos, tabla LISTA con scroll táctil y Kanban sin scroll/rebote horizontal infinito. Se memoizaron filtros/etapas y lookups de leads. CONVERSACIONES ahora virtualiza su lista para cuentas de ~2,000 chats, permite reabrir el mismo chat en móvil y actualiza mensajes cada 8 s sin vaciar ni saltar el historial. Se agregó punto verde de no leído compartido, basado exclusivamente en el último mensaje del prospecto; migración requerida: `supabase/migration_conversaciones_visto.sql`.
- **Revisión cualitativa de 30 conversaciones (2026-08-29):** los prospectos suelen indicar el programa desde el inicio y sus preguntas reales se concentran en costos, horarios, edades, ubicación, documentos y cupo. El correo no debe ser una etapa del Kanban. Pendiente validar un flujo centrado en contactos/seguimientos (`Contacto 1–3`) y siguiente paso, sin modificar todavía el bot.
- **Respuestas rápidas e ícono PWA (2026-08-29):** en CONVERSACIONES → ⚡ se agregaron “Oferta educativa” y “Contacto y dirección”; los procesos de inscripción quedaron identificados como “Cursos libres” y “Licenciaturas (en línea)”. La burbuja del asistente subió 72 px para no cubrir el envío. El ícono instalable usa ahora el logo Windsor blanco sobre fondo azul, con rutas nuevas para evitar caché en iPhone. Cambios desplegados a producción.
- **Panel de Reactivaciones sugeridas — hecho (2026-09-03):** ya no admite texto libre; obliga a elegir uno de los templates aprobados por Meta (preview de solo lectura, sin auto-envío). Además se agregaron filtros por Fuente y por Oferta educativa (desglosado por programa) en Kanban/Lista, y se resolvió el bloqueante de `META_WHATSAPP_BUSINESS_ACCOUNT_ID` faltante en Vercel. Detalle completo en `PROYECTOS.md` (carpeta madre) → sesión 2026-09-03.
- **Campaña `lic_mkt` (2026-09-03):** nuevo template de Meta para sondear interés en abrir grupo de Relaciones Públicas y Mercadotecnia, enviado a 32/33 leads del programa (excluido 1 que canceló inscripción). Script: `scripts/campana-lic-mkt.mjs`.
- [ ] **Campaña `windsor_nuevo_ciclo` — EN CURSO, 1,449/~2,261 enviados (2026-09-09):** template aprobado por Meta. Reactiva a TODOS los leads con WhatsApp (texto genérico a propósito), excluyendo `inscrito`/`perdido`/`cerrado` (`cerrado` = nombre legacy de `inscrito`, ver `LEGACY_STAGE_MAP` en `app/crm.jsx`) y RR.PP./Mercadotecnia (ya contactados con `lic_mkt`). Texto: "Hola {{1}} 👋 Se acerca el nuevo ciclo escolar en Instituto Windsor y tenemos promociones vigentes para ti. Por favor, selecciona una opción." Botones: "Me quiero inscribir" / "Tengo dudas" / "Quizás más adelante" / "Ya no me interesa". Auto-clasificación en el webhook (buscar "windsor_nuevo_ciclo" y "seguimiento_general"): → `inscripcion_pendiente` / `tercer_contacto` / `archivado` / `perdido` respectivamente. Al enviar, el script mueve el lead a `promocion_enviada`.
  - **Ritmo:** Día 1: 50 ✅ | Día 2: 100 ✅ | Día 3: 500 ✅ (saltado de 200) | Día 4: 799 ✅ (2026-09-09, 1 fallo genérico en un número de prueba/"harold", no reintentado aún — el script lo reintentará solo en la próxima corrida porque los fallos no se guardan en el log de enviados) | Quedan **812 pendientes**.
  - **Calidad del número confirmada en WhatsApp Manager antes del lote de 799 (2026-09-09):** +52 1 747 472 8775 "Instituto Windsor" — Conectado, calidad **High**. Sin señales de bloqueo pese a los lotes grandes.
  - **Resultados acumulados tras los 1,449 enviados (medido 2026-09-09):** **121/1,449 respondieron directo (8.4%)**, con mensaje real entrante del lead tras el envío (no solo cambio de stage). Por día de envío: 4-sep 10.0%, 5-sep 17.0%, 7-sep 11.0%, 9-sep 5.5% (recién enviado, sube con el tiempo). Botones: "Me quiero inscribir" 34, "Tengo dudas" 36, "Quizás más adelante" 5, "Ya no me interesa" 3. Texto libre: 43. Negocio real ya confirmado: **6 inscritos + 2 cerrados**, 36 en `inscripcion_pendiente`, 46 en `interesado`. Programas más fuertes: Inglés para adultos (12 en inscripcion_pendiente/inscrito/cerrado), Inglés para niños (8), Licenciatura en Inglés (6), Psicología (5).
  - **🐛 BUG encontrado (2026-09-09/10), sin corregir — botón "Tengo dudas" no clasifica al stage correcto:** de los 38 leads que tocaron exactamente "Tengo dudas" (el botón de `windsor_nuevo_ciclo`), solo 3 llegaron a `inscripcion_pendiente` (porque después también escribieron "me quiero inscribir"); **29 cayeron en `interesado`** en vez de `tercer_contacto` como debería. Causa: el interceptor en `app/api/whatsapp/webhook/route.ts:3376` solo hace match con el texto **"Sí, tengo dudas"** (con el "Sí,"), pero el botón real de la plantilla manda exactamente **"Tengo dudas"** (sin el "Sí,") — nunca coincide. El comentario en las líneas 3409-3411 afirma que ese caso "ya lo maneja el interceptor de arriba", lo cual es falso. El bot igual responde bien (cae al clasificador general de IA), así que no se pierde la conversación — solo que la columna `tercer_contacto` del Kanban queda vacía y esos leads terminan repartidos en `interesado`. Los otros 3 botones (`Me quiero inscribir` 30/38 correcto, `Ya no me interesa` 3/3 correcto, `Quizás más adelante` 4/5 correcto) sí funcionan bien. **Pendiente decidir con Harold:** ¿corregir el regex ahora (agregar el patrón exacto "Tengo dudas") o dejarlo así ya que funcionalmente no rompe nada, solo la etiqueta en el Kanban? — sin resolver al cierre de esta sesión.
  - **Próximo paso al retomar:** decidir sobre el bug de "Tengo dudas" de arriba; lanzar el resto (812 pendientes, probablemente en 1 lote final ya que la calidad del número sigue "High") y remedir tasa de respuesta con el total.
  - Script se corrió también para responder dudas puntuales de leads reales vía WhatsApp directo (no parte de la campaña masiva): **Angelica** (Psicología: aclarado que es semestral no cuatrimestral, re-inscripción semestral, vespertino cerrado este periodo, credencial $100) y **David Marta** (se le mandó el link de Drive del RVOE de Inglés que Harold proporcionó).
- **Bug encontrado y corregido (2026-09-04) — leads de "verano" recibían inscripción a My Best Summer ya cerrado:** al decir "quiero inscribirme"/"quiero apartar mi lugar", cualquier lead con curso de verano recibía el proceso de inscripción activo a *My Best Summer 2026*, que según `reglasNegocio.ts` ya concluyó y el bot nunca debe ofrecer. Detectado vía la bandera 🚩 (lead Tania Itzel, primer caso real usando la bandera). Corregido y desplegado: `INSCRIPCION_VERANO_NINOS_MSG`/`INSCRIPCION_VERANO_ADULTOS_MSG` en `app/api/whatsapp/webhook/route.ts` ahora redirigen explícitamente al curso regular de idiomas (abierto todo el año). **Pendiente decidir:** ¿le mando yo un mensaje de corrección a Tania con la info real, o lo hace Harold? — sin resolver al cierre de sesión.
- **Bug encontrado y corregido (2026-09-04) — fase `inscripcion` repetía el checklist ante cualquier pregunta:** en Bachillerato/Diplomado/Habilidades Psicoterapéutica/Idiomas, el bot reenviaba el checklist fijo de documentos sin importar qué preguntara el lead después (caso real: Naomi Yamileth Dimas Arcos preguntó por horarios y recibió el checklist de nuevo). Corregido con el mismo escape "si parece pregunta → cae a GPT" que ya usaba `inscripcion_pendiente`. Desplegado.
- **Bandera 🚩 de mensajes con error — ahora persistente (2026-09-04):** antes vivía solo en memoria del navegador (`useState`), se perdía al refrescar. Migración `supabase/migration_mensajes_marcado_error.sql` corrida, columna `marcado_error`/`marcado_error_at` en `whatsapp_mensajes`. Al marcar un mensaje en CONVERSACIONES queda guardado y consultable — es el mecanismo para que Harold reporte bugs del bot y Claude los revise después (así se encontró el bug de "verano"/My Best Summer arriba).
- **Respuestas rápidas de horarios agregadas (2026-09-04):** grupo "Horarios" en CONVERSACIONES → ⚡, con datos exactos de `reglasNegocio.ts` (Inglés adultos/niños, Italiano, Licenciaturas, Bachillerato matutino). Francés y Bachillerato vespertino derivan a asesor por no tener horario confirmado en la base de conocimiento.
- **UX de la tarjeta del lead en CONVERSACIONES (2026-09-05):** en `components/crm/ConversationsPanel.jsx`, se reordenó la card "Lead" (nombre → oferta educativa → teléfono → email) y se agregó botón "⧉ Copiar datos" que copia nombre+oferta+teléfono (sin email, ya que el email solo se usa para email marketing/públicos de FB Ads, el asesor no lo necesita) al portapapeles para pasárselo a un asesor por WhatsApp. La card "Responsable" ahora muestra "Modo: Bot" o el nombre de quien tomó la conversación (antes decía "Sin dueño" en negritas, se confundía con el asesor asignado al lead) → asignado → stage → fecha de entrada. El header del chat abierto ahora también muestra el tiempo restante antes de que cierre la ventana de servicio de 24h de WhatsApp (reusa `tiempoRestanteVentana()`, ya existía para la lista). Desplegado a producción, commit `e1634be`.
- **5 bugs del bot revisados y corregidos vía bandera 🚩 (2026-09-06):** Harold marcó 6 mensajes con error en CONVERSACIONES, se analizaron con su contexto completo:
  - **Idiomas para niños perdía el contexto (Estela):** pidió "idiomas para niños" desde el primer mensaje, pero tras dar su nombre el bot ofrecía el catálogo de *adultos* (Inglés adultos/Francés/Italiano). Causa: `mensajeCategoriaInteres()` en `app/api/whatsapp/webhook/route.ts` no revisaba si el mensaje original decía "niños". Corregido: ahora detecta "niños/kids" en el historial y ofrece directo *Inglés para niños*.
  - **Inscripción presencial de licenciaturas negada (Francisco):** el bot dijo "la inscripción se realiza en línea, no de forma presencial", falso — sí existe ese flujo (`buildInscripcionPresencialMsg`), solo que vivía únicamente en código de un paso posterior, no en `reglasNegocio.ts`, así que el GPT lo inventaba mal fuera de ese paso. Agregada regla explícita en `reglasNegocio.ts`.
  - **Selección múltiple sin manejar ("El A y el B" en disambiguación de inglés adultos/niños):** antes caía sin match y escalaba a un asesor sin necesidad. Ahora detecta la doble selección, manda la info de ambos cursos y pregunta cuál registrar como programa principal.
  - **RVOE incorrecto para Psicología:** el bot daba el RVOE `SEG/00052/2002` (SEP) también para Psicología — incorrecto, esa licenciatura está incorporada a la UAGro, no tiene ese RVOE. Corregido en `reglasNegocio.ts` (reglas de RVOE y título/certificado), diferenciando Psicología de las otras 3 licenciaturas.
  - **Respuesta de "promedio" incompleta (Graciela):** el dato del promedio de 9 para conservar el 30% en 2º año era correcto, pero faltaba aclarar siempre que el primer año el descuento aplica completo sin importar el promedio. Reforzado en `reglasNegocio.ts`.
  - **Bug de My Best Summer (Tania) — se encontró que el fix de 2026-09-04 estaba incompleto:** ese fix solo cubría el paso de "quiero inscribirme". Pero `INFO_MSGS['Cursos de verano niños'/'adultos']` (con fechas de julio-agosto e "Inscripciones abiertas") se seguía usando tal cual en varios otros sitios del webhook (info general del programa, reenvío tras capturar correo, etc.) — cualquiera de esos caminos le habría mandado a un lead la promoción de una temporada ya concluida sin pasar por "inscribirme". Corregido de raíz: esas dos entradas de `INFO_MSGS` ahora redirigen al curso regular vigente para TODOS los consumidores.
  - Todo corregido y verificado con `tsc --noEmit` + `eslint` (sin errores nuevos). **Pendiente:** probar en producción / WhatsApp real antes de dar por cerrado, y seguir sin resolver si se le manda a Tania Itzel un mensaje de corrección con la info real. **No desplegado todavía** (sin commit/push) al cierre de esta sesión.
- **Análisis de proyección de inscritos y plan de inversión (2026-09-06/07):** con datos reales del CRM (tasas de cierre por programa) + Meta Ads Manager consultado vía API (token en `windsorcrm/tokenMeta.txt`, cuenta `act_415474617591867`). Costo por lead blended ~$26-28 MXN; campañas de "Cursos de inglés" ~$13 MXN/lead. 3 escenarios de inversión calculados para una meta de 2,000 inscritos; validado con datos reales (no solo supuesto) que subir el gasto no ha subido el CPL dentro del rango ya probado, pero ese rango es mucho menor a los escenarios planteados — sin evidencia de qué pasa a escala grande. Detalle completo en `PROYECTOS.md` (carpeta madre) → sesión 2026-09-06/07.
  - **Pendiente sin resolver:** Harold mencionó que hay otros inscritos que no están reflejados en el cierre del CRM, sin terminar de aclarar si es por otro canal (total real de Windsor mayor a 101) o por leads sin actualizar el stage a "Inscrito" (tasa de cierre real más alta). Retomar al iniciar la próxima sesión.
  - **Pendiente:** decidir formato del "sistema de pronóstico rodante" (panel en el CRM / script / rutina manual) — sin decidir todavía.

- **6 bugs del bot corregidos y DESPLEGADOS (2026-09-07, commit `1efdc80`):** los 5 del lote del 2026-09-06 (arriba) + un 6º encontrado hoy: horario presencial de *Inglés para niños* Mar/Mié/Jue estaba mal (13:00-14:00, correcto es **13:30-14:30**) — corregido en `reglasNegocio.ts`, `docs/CONTEXT.md`, las respuestas rápidas de `ConversationsPanel.jsx` (2 lugares) y, más importante, en el **widget funcional** de agendar cita (`app/agendar/[vendedor]/page.jsx`) que arma la hora real de la cita. Probado en vivo por Harold en `https://crm.windsor.edu.mx/agendar/hola@windsor.edu.mx` — el horario ya sale bien.
- **Nuevos datos de Psicología confirmados por Harold (2026-09-07) — agregados a `reglasNegocio.ts`, SIN DESPLEGAR:** a diferencia de las otras 3 licenciaturas (cuatrimestrales), *Psicología* es **semestral** y su re-inscripción es **semestral**. El turno **vespertino de licenciaturas no se abrió este periodo** (ciclo que inicia sept. 2026) — antes el bot decía que el cambio de turno siempre era posible sin aclarar esto. La **credencial de estudiante cuesta $100** (antes no había precio documentado, solo que nunca está incluida). Falta commit/push.
- **Al probar el agendado en vivo, Harold encontró 2 huecos reales (2026-09-07) — nunca se habían construido, no es una regresión:**
  - **Sin sincronización con Google Calendar:** las citas de `app/agendar` solo viven en la tabla `citas` / `AgendaPanel.jsx` interno. No existe ninguna integración con Google Calendar API en todo el repo. **Pendiente decidir:** ¿calendario de `hola@windsor.edu.mx` o uno por asesor? — sin resolver, no se empezó a construir.
  - **Sin confirmación por WhatsApp al agendar**, pese a que la pantalla de confirmación lo promete ("Te enviaremos un recordatorio por WhatsApp"). **Ya implementado y verificado con tsc/eslint pero SIN DESPLEGAR:** `app/api/agendar/route.ts` ahora manda un mensaje de confirmación vía `/api/whatsapp/send` tras crear la cita (best-effort, no bloquea la reserva si falla). **Limitación real detectada:** como el link `/agendar` es público, un lead puede agendar sin haber tenido conversación de WhatsApp previa (ventana 24h cerrada) — Meta rechazaría el mensaje libre en ese caso. **Se necesita un template Utility aprobado por Meta para garantizar la entrega siempre.** Texto propuesto, pendiente de que Harold lo suba a Meta Business Suite para aprobación (~24h): "Hola {{1}} 👋 Tu {{2}} en Instituto Windsor quedó confirmada para el {{3}} a las {{4}}. ¡Nos vemos pronto! Si necesitas reagendar, contáctanos por este medio." (variables: nombre, tipo de cita, fecha, horario).
- **Nueva campaña en consideración (2026-09-07, sin decidir) — clase de prueba para leads de inglés:** Harold está evaluando mandar un texto nuevo a leads de cursos de inglés ofreciendo clase de prueba. Mencionó querer usar el link `https://confirmafy.com/iwindsor` — **no está claro si es para reemplazar el `/agendar` interno en esta campaña específica, o si Windsor va a adoptar Confirmafy como plataforma de agendado en general.** No aparece en ningún lugar del código todavía. Aclarar con Harold antes de avanzar.

*Última actualización (2026-09-07/08): 6 bugs del bot corregidos y DESPLEGADOS (RVOE Psicología + horario Inglés niños + 4 más). Nuevos datos de Psicología (semestral, vespertino cerrado, credencial $100) agregados al código pero sin desplegar. Campaña `windsor_nuevo_ciclo` en 650/~2,134 (día 3 de 6, saltado a 500/día), 9 inscritos ya. Confirmación por WhatsApp al agendar cita construida pero sin desplegar — necesita template Utility aprobado por Meta para funcionar siempre. Sin empezar: sync con Google Calendar (falta decidir de quién es el calendario). Sin aclarar: para qué es el link de Confirmafy. Sigue sin resolver: aclarar los "otros inscritos" fuera del CRM, formato del sistema de pronóstico rodante, mensaje de corrección a Tania Itzel.*

- **Revisión de banderas 🚩 y despliegue (2026-09-14):** se revisaron las nuevas respuestas marcadas y se corrigieron/desplegaron en `d10bebb` únicamente los cambios del bot: respuestas confirmadas de costos/inicio e incorporación de Inglés adultos, pago en efectivo en instalaciones, horario matutino de Bachillerato, duración/horarios/modalidad online de licenciaturas y selección “la C”; se evitó escalar el botón A de dudas y se reconoce la etiqueta legacy `Curso de Inglés` al inscribir. Correcciones manuales enviadas: **Itziar** (sí existe Licenciatura en Inglés online; se ofreció disculpa) y **Arely** (sí puede pagar en efectivo en instalaciones; se ofreció disculpa). Producción verificada con HTTP 200. No se incluyeron Calendar, citas ni Embedded Signup.
- **Respuestas rápidas editables desde el CRM — DESPLEGADO (2026-09-19, commits `f3ae5a4` y `18342c0`):** en CONVERSACIONES → ⚡ se puede agregar una respuesta rápida nueva directamente desde el menú (botón "+ Agregar respuesta": elige grupo existente, título y texto) y borrar las que agregaste (🗑), sin tocar código. Se guardan en tabla `respuestas_rapidas_custom` (Supabase, migración corrida) y se mergean dentro de los grupos ya existentes (Idiomas, Inscripción, etc.) — decisión explícita de Harold de no crear una división aparte. **Bug encontrado al primer uso real y corregido en el acto (`18342c0`):** el primer commit (`f3ae5a4`) importaba el cliente de Supabase equivocado en `ConversationsPanel.jsx` (`@/lib/supabase`, key anónima sin sesión) en vez del que usa el resto del CRM (`@/utils/supabase/client`, con la sesión del usuario vía cookies) — las políticas RLS de la tabla exigen `auth.role() = 'authenticated'`, así que el insert/delete se rechazaba siempre. Corregido y verificado en producción.
- **Revisión de banderas 🚩 y despliegue (2026-09-17), commit `9321fa6`:** de 33 mensajes marcados, ~20 ya estaban resueltos por commits anteriores (flags viejos sin limpiar). Se corrigieron y desplegaron 6 bugs reales: (1) el prompt de GPT no tenía la fecha de hoy y alucinaba meses de vigencia de promo (caso Arlette, "solo agosto 2026" estando en septiembre); (2) **bug grave** — la fase `cerrado` (plática terminada cordialmente) promovía el stage del lead a `cerrado`, que el CRM pinta como "✅ Inscrito" sin que hubiera venta real (verificado: 19 leads afectados, ~36% del total de "Inscritos" mostrados eran falsos) — `getStageForPhase()` ya no toca el stage ahí; (3) escalación genérica cuando el lead decía "me quiero inscribir" sin programa identificado (placeholder "WhatsApp - Instituto Windsor") — ahora pregunta cuál programa; (4) `esInglesIdioma()` no reconocía `curso="Inglés"` a secas; (5) documentos de niño para curso de inglés corregidos (solo acta + solicitud, no CURP/fotos de licenciatura); (6) el bot negaba que hubiera clase de prueba (sí existe, vía examen de ubicación gratuito). Extra: regex de "ya pagué/listo" acotado (la palabra suelta "listo" disparaba falsas alertas de inscripción confirmada), y lista de "STAGES POSIBLES" del asistente admin (comando `CRM...`) actualizada — le faltaban la mitad de los stages reales del Kanban. **Pendiente sin tocar (Harold lo limpia poco a poco):** reclasificar los 19 leads con `stage=cerrado`, y desmarcar los ~20 flags 🚩 viejos ya resueltos.
- **Google Drive conectado (2026-09-17):** primera vez que se conecta el MCP de Google Drive en una sesión de Claude Code para este proyecto (cuenta hola@windsor.edu.mx). Carpeta de planes de estudio de diplomados ya identificada y accesible: https://drive.google.com/drive/folders/1eni6C5Trip3oLU6qT8Xat6VRJv0wAchS — un PDF por diplomado (Psicología educativa, Tecnología educativa, Administración RH, etc.), todos con permiso "cualquiera con el link puede ver". Usado para resolver un pendiente real: Paulina (+527712388693, Diplomado en Psicología Educativa) había pedido el "plan de estudios" y se le quedó sin contestar al pasar la conversación a modo humano — se le mandó el link del PDF por WhatsApp vía `/api/whatsapp/send`. **Pendiente sin resolver:** el asesor le dio a Paulina un precio de 3 pagos × $3,100 (total $9,300) que no coincide con el PDF oficial (3 pagos × $3,000, total $9,000); tampoco se le mencionó la opción de pago único de $8,370 (la más barata). Falta decidir si se le corrige el dato.
- **Caso Alina López (+527471592228) diagnosticado y atendido a mano (2026-09-19) — reveló bug nuevo sin corregir:** Harold pidió revisar por qué no le llegó la info a esta lead. Encontrado con datos reales (timestamps de Supabase): el lead se creó manual, a los 2 min se movió a stage "Inscrito" en el pipeline, y 30 min después Alina tocó "Sí, envíame la info" del template de bienvenida — el bot nunca contestó, solo registró `lead_activities: lead_perdido_reescribio` y avisó al asesor asignado. Causa raíz ver arriba (sección Técnico/Producto, bug "Inscrito cierra conversación") — **no es el mismo bug ya corregido el 17-sep**, es la dirección opuesta y sigue sin arreglar. Se le mandó la info de Licenciatura en Inglés a mano desde el CRM (dentro de la ventana de 24h) mientras se decide el fix definitivo.
